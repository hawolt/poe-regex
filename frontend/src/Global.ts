// Populated at load time by build() in main.ts using shared group intersections from map.mod.config.json.
// Structure: [modifierIndex, [associatedModifierIndex, ...]]
export const associations: [number, number[]][] = [];